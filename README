Hydranode
=========

Server usage:
  server.js
    var hydranode = require('./src/hydranode');
    var server = new hydranode.server.Server;
    server.start();

  $ node server.js

Client usage:
  client.js
    var hydranode = require('./src/hydranode');
    new client.Client('localhost', 8124, function() {
      /* do stuff */
    });

  $ node client.js
